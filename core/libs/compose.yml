#@ def extend(name):
file: #@ '${dev_ops_path}/envs/' + current_env + '/.' + name + '.yml.tmp'
service: #@ name
#@ end

#@ def busybox():
#@ return extend('busybox')
#@ end

#@ def mockserver():
#@ return extend('mockserver')
#@ end

#@ def mysql(ver):
#@ return extend('mysql' + str(ver))
#@ end

#@ def node(ver):
#@ return extend('node' + str(ver))
#@ end

