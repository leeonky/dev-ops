#@ def imageName(service):
#@ return service + '.${current_env}${current_task}.${PROJECT_NAME}'
#@ end

#@ def isParellel():
#@ return task_count != '1'
#@ end

#@ def hostUser():
#@ return "${CONTAINER_UID-0}:${CONTAINER_GID-0}"
#@ end
