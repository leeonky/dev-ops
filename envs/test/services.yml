version: '3.4'

services:
  busybox: 
    extends: #@ busybox()
  mysql:
    extends: #@ mysql(8)
  mockserver:
    extends: #@ mockserver()
  node:
    extends: #@ node(14)
  jdk:
    extends: #@ openjdk(11)

